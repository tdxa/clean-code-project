import { secondaryColor } from '../../../../styles/mui/mui-variables';

const muiStylesSnackbar = {
  container: {
    background: secondaryColor,
    color: fontColorSnackbar,
    boxShadow: shadowSnackbar,
    width: '370px',
    fontSize: '14px',
  },
  icon: {
    color: activeElementYellow,
  },
};

export default muiStylesSnackbar;
